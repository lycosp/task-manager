export interface User {
    id: number;
    username: string;
    privilegeId: number;
    privilege: string;

    // constructor(id: number, username: string, privilegeId: number, privilege: string) {
    //     this.id = id;
    //     this.username = username;
    //     this.privilegeId = privilegeId;
    //     this.privilege = privilege;
    // }
}